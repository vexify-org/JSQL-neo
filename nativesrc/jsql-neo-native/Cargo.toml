[package]
name = "jsql-neo-native"
version = "5.4.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jsql-neo-core = { path = "../jsql-neo-core" }
napi = { version = "2", features = ["napi6"] }
napi-derive = "2"
serde_json = "1"

[build-dependencies]
napi-build = "2"
